language: ruby
rvm:
  - 2.3.5
cache:
  - bundler
  - yarn
dist: trusty
env:
  - MOZ_HEADLESS=1
addons:
  firefox: latest
before_script:
  - psql -c 'create database ferret_test;' -U postgres
script:
  - bin/rails assets:precompile
  - bin/rails test:system
  - bin/rails test
  - bundle exec rspec
